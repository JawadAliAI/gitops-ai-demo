# Issue #1: Add a CONTRIBUTING note to the README

This note was generated by the GitOps AI stub agent because no LLM provider is configured (`ANTHROPIC_API_KEY` is unset). Configure an LLM provider in `.env` to have the agent generate a real code fix instead of this placeholder.

## Original issue body

The README currently has no contribution guidance.

Please add a short section titled  at the end of README.md with one or two sentences inviting pull requests.

```markdown
## Contributing
Pull requests are welcome!
```

